dependencies:
  pre:
    - go version
  override:
    - make get-deps
    - make build
  post:
    - cp build/* $CIRCLE_ARTIFACTS

test:
  override:
    - /bin/true

deployment:
  release:
    branch: release
    commands:
      - make release